#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../grunt-groundskeeper/bin/grunt-groundskeeper" "$@"
  ret=$?
else 
  node  "$basedir/../grunt-groundskeeper/bin/grunt-groundskeeper" "$@"
  ret=$?
fi
exit $ret
